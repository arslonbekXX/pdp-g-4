export interface HomeProps {}

export interface Language {
  id: string;
  title: string;
  url: string;
}
export interface Section {
  id: number;
  title: string;
  url: string;
  description: string;
  maxRate: number;
}
