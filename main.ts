function getUser(userID: string) {
  return new Promise((resolve, reject) => {});
}

function app(firstName: string) {}

app("arslonbek");

// WEB API ->  V8 Engine (Fetch, Promise, Date, time)  (C++)
// JS ->  let, const, for, while

/**
 * -----JavaScript-----
 *  ✅ reference (object, function, array) (Stack || Heap)
 *  ✅ Web API
 *  ✅ Proto && Prototype (Class, Function)
 *  ✅ Event Loop
 */
